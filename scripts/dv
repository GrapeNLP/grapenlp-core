#!/bin/bash
cd `dirname $0`
. paths

echo -n "to_fprtn & breadth-first expand: "
$DEBUG_GRAPENLP -xwc $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "to_fprtn & breadth-first expand with trie strings: "
$DEBUG_GRAPENLP -xwct $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "earley: "
$DEBUG_GRAPENLP -xwc -pearley $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "breadth-first: "
$DEBUG_GRAPENLP -xwc -pbreadth-first $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "breadth-first with trie strings: "
$DEBUG_GRAPENLP -xwct -pbreadth-first $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "depth-first: "
$DEBUG_GRAPENLP -xwc -pdepth-first $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
echo -n "depth-first with trie strings: "
$DEBUG_GRAPENLP -xwct -pdepth-first $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $OUTPUT $VALIDATE | grep -E "Validation|Invalid"
