#!/bin/bash
cd `dirname $0`
. paths

if [ "$1" = "m" ]
then
$RELEASE_GRAPENLP -mxw $CHATBOT_GRAMMAR $COMPRESSED_DICO $TEST_INPUT $OUTPUT
else
$RELEASE_GRAPENLP -xw $CHATBOT_GRAMMAR $COMPRESSED_DICO $TEST_INPUT $OUTPUT
fi
