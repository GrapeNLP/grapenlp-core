#!/bin/bash
cd `dirname $0`
. paths

RARG="1s"

$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-zpps -estd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-zpps -elrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-zpps -elrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn -estd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn -elrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn -elrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -estd -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -estd -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -estd -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -estd -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -estd -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -estd -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pto-fprtn-and-breadth-first-expand -elrb-tree-3w -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -estd -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -estd -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -estd -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree-3w -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree-3w -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pearley -elrb-tree-3w -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -snxwc -pearley -estd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwc -pearley -elrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwc -pearley -elrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -estd -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -estd -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -estd -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -estd -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -estd -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -estd -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree-3w -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree-3w -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree-3w -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree-3w -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pbreadth-first -elrb-tree-3w -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pbreadth-first -elrb-tree-3w -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -snxwc -pbreadth-first -estd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwct -pbreadth-first -estd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwc -pbreadth-first -elrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwct -pbreadth-first -elrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwc -pbreadth-first -elrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwct -pbreadth-first -elrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -sxwc -pdepth-first -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pdepth-first -bstd -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pdepth-first -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pdepth-first -blrb-tree -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwc -pdepth-first -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -sxwct -pdepth-first -blrb-tree-3w -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'

$RELEASE_GRAPENLP --desc-parser -snxwc -pdepth-first -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
$RELEASE_GRAPENLP --desc-parser -snxwct -pdepth-first -r$RARG $CHATBOT_GRAMMAR $COMPRESSED_DICO $CORPUS_NO_OUTPUT $TEST_CONTEXT $OUTPUT | grep -E '\*\ |sentences per second' | sed 's/Average sentences per second://g' | sed 's/\*\ //g' | sed 's/\ \*//g'
